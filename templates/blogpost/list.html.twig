{% extends 'content.html.twig' %}

{% block content %}
    <ul>
        {% for blogpost in blogPosts %}
            <li>
                <a href="{{ path('showById', {id: blogpost.id}) }}">{{ blogpost.title }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}